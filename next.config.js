/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/Haxplore-main', // Replace with your repository name
}

module.exports = nextConfig 